{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        
        "php-serve": "php artisan serve --port=8080",
        "php-migrate": "php artisan migrate --seed",
        "php-migrate-rollback": "php artisan migrate:rollback",
        "php-migrate-refresh": "php artisan migrate:refresh --seed",
        "php-worker": "php artisan queue:work --queue=default"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0"
    }
}
